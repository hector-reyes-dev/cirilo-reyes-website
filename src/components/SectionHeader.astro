---
interface Props {
  tag: string;
  title: string;
  intro: string;
}

const { tag, title, intro } = Astro.props as Props;
---

<div class="section-header">
  <p class="section-tag">{tag}</p>
  <h2>{title}</h2>
  <p>{intro}</p>
</div>
